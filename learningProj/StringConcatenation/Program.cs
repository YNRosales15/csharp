string firstName = "Yessi";
string greeting = "Hello";
//string message = greeting +" " + firstName + "!";
//Console.WriteLine(message);
Console.WriteLine(greeting + " " + firstName + "!" );

// String concatenation allows to combine smaller literal and variable string into a single string
// Avoid creating intermediate variables if adding them doesn't increase readablity
